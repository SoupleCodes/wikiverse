<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Wiki Clone - Subject: <%= subject %></title>
    <style>
        body {
            font-family: serif;
            background: white;
            padding: 0;
        }
        .container {
            width: 80%;
            padding: 20px;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Articles on <%= subject %></h1>
        <ul class="article-list">
            <% articles.forEach(article => { %>
                <li><a href="/article/<%= article.id %>"><%= article.title %> by <%= article.author %></a></li>
            <% }) %>
        </ul>
        <a href="/">Back to Home</a>
    </div>
</body>
</html>